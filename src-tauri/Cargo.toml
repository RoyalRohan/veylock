[package]
name = "veylock"
version = "1.1.0"
description = "Veylock — Privacy-first, local-only password manager for Windows and Linux."
authors = ["Veylock Open Source Contributors"]
license = "MIT"
edition = "2021"

[lib]
name = "veylock_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
argon2 = "0.5"
aes-gcm = "0.10"
rand = "0.8"
zeroize = { version = "1.7", features = ["zeroize_derive"] }
rusqlite = { version = "0.31", features = ["bundled"] }
totp-lite = "2.0"
uuid = { version = "1.8", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
base64 = "0.22"
